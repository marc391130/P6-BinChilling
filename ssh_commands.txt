#Slet en mappe fra termina(Linux)
#Tilføj sudo for at gøre det som admin
#TIlføj -f for at køre det med force mode (aka hvis det bliver brugt)

rm -r Path/To/Folder

#Kør følgende på serveren for at genererer en python requirement folder
#Stå i p6 mappen
pipreqs

#Stå i github mappe, kør:

scp -r ./P6 P6:/mnt/bak/P6 